# 測試報告

執行日期：2026-09-15（Asia/Taipei）
環境：macOS、Node.js 24.14.0、Vitest 3.2.7、Vite 7.3.6、Codex in-app Chromium。

## 最新結果

- 自動測試：2 個測試檔、29 項測試通過、0 失敗。
- 正式建置：37 個 modules transformed，成功產出 GitHub Pages 用 `dist/`。
- 新版集點卡：1748 × 1240；高解析輸出設定為 3496 × 2480。
- 新版印章：七張來源 PNG 均為 RGBA 且保留原檔；顯示與輸出皆由設定套用順時鐘 14.9°。
- 本機瀏覽器：已檢查 0/7、逐站加入至 5/7、五枚完成門檻、完成頁投稿入口，以及既有 7/7 完成狀態；未見圖片載入失敗。
- 投稿 client：已驗證 API URL、五個 multipart 欄位、穩定 `Idempotency-Key`、未手動設定 `Content-Type`、成功回應格式及 429 錯誤保留。
- 正式 API：`GET /healthz` 回傳 HTTP 200 `{"status":"ok"}`；從 `https://hunghaoming.github.io` 對投稿 endpoint 的 CORS preflight 回傳 HTTP 200，允許 POST 與 `Idempotency-Key`。
- 正式 E2E：建立標示「Codex E2E 測試（請取消）」的 5 枚測試投稿，首次回傳 HTTP 201、兌換碼 `TPR6WR`；以相同參與者 ID／冪等鍵重送回傳 HTTP 200、相同 submission ID／兌換碼及 `updated: true`，沒有建立重複投稿。
- 完成頁：已移除手機直接列印按鈕與驅動提示；三語主要操作皆指向總關 Server，PNG 下載、Web Share 與開圖儲存仍保留。
- 公開前端：已移除 PIN、補發／移除印章及網頁 QR 管理介面；`?staff=1`、`?view=qr` 會回到一般參加者流程。
- QR 產生：本機指令已成功產生七張 1040 × 1040 RGBA PNG 與網址清單，不部署操作介面到 GitHub Pages。
- 正式產物檢查：未含舊 PIN 文案／控制流程、完成頁列印、驅動提示或補章介面的識別字串；`git diff --check` 通過。
- 七件作品內容：已加入活動方提供的中、英、日正式名稱與介紹，以及中文圖片替代文字；未提供的作者／團隊／單位與補充資訊欄位不保留，也不從海報推定。
- 作品介紹圖：八張 1894 × 1578 PNG 均以原檔副本保存；七個作品頁全部載入成功，磁吸式智慧電路教具在 390 × 844 viewport 以單欄顯示兩張圖，沒有水平溢位。
- 領章結果頁：取得新印章與重複領章均顯示對應的正式作品介紹圖，不再誤用透明印章縮圖；卡片預覽與完成圖仍正確使用既有透明印章。
- 卡片預覽：已在隔離的全新瀏覽器 origin 檢查 0/7 與 7/7；七枚既有透明印章、順序、位置與完成門檻維持不變。
- 預設語言：新建立的瀏覽器狀態使用繁體中文；既有狀態中有效的英文或日文選擇仍由 `normalizeState` 保留。

## 自動測試範圍

| 測試 | 結果 |
|---|---|
| 英文、繁體中文、日文暱稱與 20 字元限制 | 通過 |
| 控制字元清理與 HTML 安全輸出相關資料處理 | 通過 |
| 鍵盤留言 100 字元限制 | 通過 |
| 站點 ID 與專屬 token 必須同時正確 | 通過 |
| 七章亂序加入、完成時間與重複章阻擋 | 通過 |
| `localStorage` 儲存後重新載入 | 通過 |
| 舊資料或遭修改資料的安全正規化 | 通過 |
| 底層 JSON 進度匯出、匯入及錯誤格式阻擋（公開頁無入口） | 通過 |
| 七組素材、唯一 token、百分比座標、14.9° 旋轉與排序 | 通過 |
| 三語介面 key 完整且非英文回退內容 | 通過 |
| 三語完成頁不含直接列印與工作人員文案 | 通過 |
| `?staff=1`、`?view=qr` 不開啟管理模式 | 通過 |
| 本機 QR 產生網址保留部署子路徑、清除舊 query/hash | 通過 |
| 隨機參與者 ID／冪等鍵保存及污染值替換 | 通過 |
| 總關 multipart 投稿、冪等 header、成功及錯誤回應 | 通過 |

## 瀏覽器實測紀錄

舊版六站版本曾以 360 × 740 及 390 × 844 viewport 完成下列流程；以下三項是歷史測試紀錄，不代表目前站點數：

- 首次建立暱稱，以及先掃 QR 再建立暱稱。
- 六章不同順序、重複章、錯誤 token。
- 重新整理後保留暱稱、印章、手寫及鍵盤留言。
- 舊版公開工作人員 PIN、補章、移除與 QR 管理頁（已移除，不代表目前功能）。
- 第六章完成頁、手寫保存、100 字留言、Canvas 合成。
- GitHub Pages 子路徑資源與缺圖友善提示。

2026-09-14 已在本機 Chromium 完成新版卡片的 0/7 與 7/7 視覺檢查。七枚來源圖片各自加入成功，預覽與 3496 × 2480 Canvas 合成圖皆以白格中心定位並順時鐘旋轉 14.9°。姓名已落在左上 NAME 線區；手寫／繪圖／鍵盤文字區的最終位置仍須由專案擁有者確認。

同日另完成 5/7 流程：五枚時已出現完成卡入口，完成頁標題正確顯示目前印章數，並顯示「送到總關」按鈕。正式 API 已驗證 201 新增與 200 冪等更新；仍須由工作人員登入後台確認測試投稿 `TPR6WR` 可預覽並取消。

2026-09-15 以 390 × 844 viewport 檢查 5/7 完成頁：主要按鈕為送到總關 Server，PNG 下載、Web Share、開圖儲存均保留，未顯示手機列印或驅動提示。另以瀏覽器實際開啟 `?staff=1` 與 `?view=qr`，兩者都顯示一般參加者卡片，沒有工作人員或 QR 管理介面；console 無警告或錯誤。

## 仍須真實手機驗證

- iPhone Safari：相機權限與七站掃碼、手寫觸控、旋轉、PNG 下載、開圖長按存照片、Web Share、關閉再開、短暫斷網。
- Android Chrome：相機權限與七站掃碼、手寫觸控、旋轉、PNG 下載、Web Share、關閉再開、短暫斷網。
- 實際印刷 QR Code 的辨識距離與速度。
- 總關 Server 收件後的實際列印、235 gsm 進紙、色彩及邊緣裁切。
- 3496 × 2480 完成圖在手機相簿中的文字清晰度、透明印章邊緣與三語字型。
- 連續兩次重置確認與清除手寫確認的真機操作手感。
- 正式 API 的 201 新投稿、200 pending 更新、兌換碼顯示，以及 400／413／415／422／429 實際回應。

## 已知內容待確認

- 七個作品頁的正式三語內容、介紹圖及 `animal01`～`animal07` 對應，等待專案擁有者確認本次預覽。
- 未提供的作者／團隊／單位與補充資訊不建立欄位，也不顯示空白區塊。
- 姓名、手寫／繪圖及鍵盤文字的最終百分比座標。
- 展前 P0：Service Worker 舊快取可能讓已開過網站的瀏覽器在新版部署後載入舊入口 HTML；需修正更新策略並以既有瀏覽器驗證。
- 展前 P0：總關 `/admin/` 在 session 過期時目前回傳 JSON 錯誤，需由 Server 專案改為後台頁面導回 `/admin/login`。
