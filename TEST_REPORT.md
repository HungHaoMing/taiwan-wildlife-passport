# 測試報告

執行日期：2026-07-26（Asia/Taipei）  
環境：Windows、本機 Node.js、Vitest 3.2.7、Vite 7.3.6、Codex in-app Chromium browser，手機 viewport 360 × 740。

自動測試實際結果：`1 passed` test file、`8 passed` tests、`0 failed`。正式建置實際結果：58 modules transformed，成功輸出 `dist/`；主程式 gzip 19.35 kB。套件安裝檢查為 0 vulnerabilities。

## 自動測試對照

| 驗收情境 | 驗證方式 | 結果 |
|---|---|---|
| 首次建立暱稱、三語字元與 20 字限制 | 單元測試；360px 瀏覽器輸入「測試 Student あ」 | 通過 |
| 先掃碼再建立暱稱 | 全新 origin 先開 Animal 2 QR，再輸入「QR First Fixed」 | 通過；補章後顯示動畫與動物介紹 |
| 六章不同順序 | 單元測試 `[5,2,6,1,4,3]`；瀏覽器 `[2,6,1,4,3,5]` | 通過，卡片為 6 / 6 |
| 重複掃同一章 | 單元測試；瀏覽器重掃 Animal 5 | 通過，顯示 already have 提示且不重複新增 |
| 錯誤 token／ID | `validateStamp` 正反測試；瀏覽器使用 Animal 2 + `WRONG` | 通過；實測曾找出既有暱稱不顯示錯誤，已修正並重測 |
| 重新整理／關閉再開進度保留 | Web Storage save/load；瀏覽器 reload 暱稱、六章、手寫與留言 | 通過；真正關閉 App 後重開仍列入真機測試 |
| 第六章完成時間與完成流程 | 狀態測試；最後掃 Animal 5 | 通過；1.8 秒章動畫／介紹後自動進完成流程 |
| 手寫、清除、復原 | 360px 觸控拖曳軌跡；程式確認清除與 undo 路徑 | 手寫與重載復原通過；原生確認框自動化受瀏覽器工具限制，真機再驗清除確認手感 |
| 100 字鍵盤留言 | 單元測試；瀏覽器填入 110 字並保存、reload | 通過，實際保留 100 字 |
| 完成 PNG 合成 | 六章 + 暱稱 + 手寫 + 100 字留言產生 Canvas | 通過；開圖替代流程產出約 609,770 字元 PNG data URL。自動化瀏覽器未回報 blob download event，真機下載仍待驗 |
| 工作人員補章／移除 | PIN 登入，移除 Animal 3 成為 5 / 6，再補回 6 / 6 | 通過 |
| 重置二次確認 | 原始碼與瀏覽器 confirm 顯示路徑 | 兩層 `confirm` 已實作；自動化工具無法穩定接管連續原生對話框，列入真機複驗 |
| JSON 匯出／匯入 | round-trip 與錯誤格式單元測試 | 通過 |
| QR 管理及列印頁 | 瀏覽器檢查卡片與 canvas 數量 | 通過；6 張 QR、6 個含 token 網址 |
| 六組圖片、token、相對位置 | 集中設定完整性測試 | 通過；6 個唯一且至少 16 字 token |
| GitHub Pages 子路徑 | 正式 build 以 `/passport/` 預覽 | 通過；0 asset console error，QR 網址保留 `/passport/` |
| 窄螢幕 | viewport 360 × 740 完整流程 | 通過；按鈕、表單、畫布可操作 |
| 缺圖處理 | 暫時將 Animal 1 指向不存在檔案並進介紹頁 | 通過；顯示檔名與可讀提示，設定已還原 |

## 必須以真實手機再驗證

- iPhone Safari：相機掃六站 QR、Pointer Events 筆跡、旋轉、PNG 下載、開圖長按存照片、分享面板、關閉分頁再開、短暫斷網。
- Android Chrome：相機掃六站 QR、觸控筆跡、旋轉、PNG 下載、Web Share、關閉分頁再開、短暫斷網。
- 使用最終正式底圖與六張透明印章，逐一校正位置、清晰度、透明邊緣、三語字型及實際列印 QR Code 可讀性。
- 實際缺少一張正式素材時，確認現場可理解錯誤提示且其餘流程不受影響。

## 測試中發現並修正

1. 已有暱稱時掃到錯誤 token，原本直接回卡片而未提示；新增獨立無效連結畫面後重測通過。
2. 第一次由 QR 進站並送出暱稱時，原本有寫入印章但未立即渲染結果頁；補上渲染後重測通過。
3. 已保存手寫後再加一筆並復原，原畫可能被一起清除；畫布新增 base image 層後，復原只移除新筆畫。
